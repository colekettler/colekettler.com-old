---

- name: install exim4
  apt:
    name: exim4
    state: present

- name: configure exim4
  template:
    src: templates/update-exim4.conf.conf.j2
    dest: /etc/exim4/update-exim4.conf.conf
  notify:
    - reconfigure exim4
    - restart exim4
